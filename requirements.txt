# Single requirements file for the whole AnyTime monorepo.
# Covers benchmark + training across all 4 model families:
#   AnyTimeBert / AnyTimeLLaMa / AnyTimeVisionenc / AnyTimeYolo
# (AnyTimeBert reference code was patched to work under transformers 5.x,
#  so the legacy 4.6.1 split-venv is no longer needed.)
#
# Vendor sub-trees ship their own pinned requirements and are NOT folded in:
#   - AnyTimeYolo/model/yolov9/requirements.txt
#   - LayerSkip/requirements.txt
# Install those into separate venvs if you need to run their upstream scripts.

# ----- core ml stack ---------------------------------------------------------
# torch / torchvision / torchaudio: install MANUALLY before this file.
# Pick the wheel that matches your driver + GPU:
#   Blackwell (RTX 50xx, sm_120) -> cu128 / cu129 / cu130
#   Ampere/Ada -> cu121 / cu124 / cu126
# Example (Blackwell, driver R580+):
#   pip install --index-url https://download.pytorch.org/whl/cu130 \
#       torch torchvision torchaudio
transformers>=5.0            # 5.x drops TF/Flax; PyTorch-only
accelerate
safetensors
datasets
huggingface_hub[cli]
tokenizers
pydantic

# ----- HW profiling (shared) -------------------------------------------------
psutil
nvidia-ml-py                 # official NVML; replaces deprecated `pynvml`

# ----- I/O + UX --------------------------------------------------------------
tqdm
numpy
pandas
scikit-learn

# ----- model-specific (only needed for that model's bench) ------------------
# BERT (GLUE)
fitlog                       # ElasticBERT reference dep

# LLaMa
rouge-score                  # quality eval
kernels<0.11.3               # transformers compatibility pin

# Vision (MSDNet)
# torchvision already covers CIFAR/SVHN/ImageFolder loaders

# YOLO
ultralytics                  # YOLOv9 utility helpers
opencv-python
matplotlib
seaborn
roboflow                     # dataset pull

# ----- jupyter (notebooks) ---------------------------------------------------
jupyter
ipykernel
